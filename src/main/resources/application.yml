spring:
  application:
    name: GuandanGame
  datasource:
    url: jdbc:mysql://localhost:3306/guandan?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: mysql/Gth20050705
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.guandan.entity
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080

agent:
  server:
    url: ws://192.168.1.4:8000/ws
    timeout: 5000
